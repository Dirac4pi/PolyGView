# gview_for_xyz

&nbsp;&nbsp;&nbsp;&nbsp;XYZ files are the most commonly used in quantum
chemistry calculations and first-principle material calculations. Once you place
these scripts in the GaussView directory and configure the environment, you can
visualize and model XYZ files in the most convenient way.<br>
&nbsp;&nbsp;&nbsp;&nbsp;NumPy is required in the environment, and add the
following to `~/.bashrc`:

```shell
gview() {
source /opt/g16/bsd/g16.profile
python3 /path/to/gv/gview.py "$@"
}
alias gjf2xyz='python3 /path/to/gv/gjf2xyz.py'
alias xyzdump='python3 /path/to/gv/xyzdump.py'
alias drmsd='python3 /path/to/gv/drmsd.py'
```

## gview.py

&nbsp;&nbsp;&nbsp;&nbsp;For single-frame XYZ files, visualize and model directly
using `gview input.xyz`, similar to handling GJF files, supporting both isolated
and periodic systems. For multi-frame XYZ files, visualize directly using `gview
input.xyz`, regardless of isolated or periodic systems; it will display the
Distance RMSD and Distance MAXD for the multi-frame trajectory, similar to
handling Gaussian geometry optimization LOG files. The script also supports
viewing optimization energies with multi-frame XYZ files generated by ORCA or
CP2K programs and not affect the functions that Gaussview supports natively.<br>
&nbsp;&nbsp;&nbsp;&nbsp;If you have install
[OfakeG](http://sobereva.com/soft/OfakeG/) and
[MfakeG](http://sobereva.com/soft/MfakeG/) in GaussView directory,
gview.py can invoke them to visualize vibration modes in ORCA output files and
CP2K MOLDEN format files.<br>
&nbsp;&nbsp;&nbsp;&nbsp;If you have install
[Obabel 2.4.x](https://github.com/openbabel/openbabel/releases) and add it to
`$PATH`, gview.py can invoke it to visualize 2D structures in ChemDraw .cdxml
files.

## gjf2xyz.py

&nbsp;&nbsp;&nbsp;&nbsp;Convert GJF files to XYZ files, supporting isolated and
periodic systems. Usage: `gjf2xyz input.gjf`.<br>

## xyzdump.py

&nbsp;&nbsp;&nbsp;&nbsp;Export the last frame in a multi-frame XYZ file as a
single-frame XYZ file. If the multi-frame XYZ file was generated by CP2K, it
will export a single-frame XYZ file containing cell information. Usage:
`xyzdump multi.xyz single.xyz`.<br>

## drmsd.py

&nbsp;&nbsp;&nbsp;&nbsp;Calculate the Distance MAXD (dMAXD) and Distance RMSD
(dRMSD) between two specified conformers of the same molecule (provided via two
XYZ files), which is used to discrabe the differences between the two conformers.
dRMSD is dimensionally consistent, as well as translationally and rotationally
invariant. Usage: `drmsd conf1.xyz conf2.xyz`.<br>

$$
\begin{aligned}
\text{dRMSD} &= \sqrt{\frac{2}{N(N-1)} \sum_{i \lt j}^{N} (d_{ij,A} - d_{ij,B})^2} \\
\text{dMAXD} &= \max_{i \lt j} |d_{ij,A} - d_{ij,B}|
\end{aligned}
$$
